# yetiswarm compatibility shim

This directory is kept only for backward compatibility with legacy commands such as:

```bash
npm --prefix skill/yetiswarm run build
```

Canonical skill layout is now at repository root:

- `SKILL.md`
- `scripts/`
- `runtime/`
